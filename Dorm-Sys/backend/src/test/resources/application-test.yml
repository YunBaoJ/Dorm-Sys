spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:mysql://localhost:3306/dormitory_test?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai}
  sql:
    init:
      mode: never

logging:
  config: classpath:logback-test.xml
  file:
    path: target/test-logs
